## 2024-05-23 - Accessibility in Shadcn Forms
**Learning:** Shadcn's Label and Input components do not automatically associate via nesting. Explicit `id` and `htmlFor` are required for accessibility.
**Action:** Always verify label-input associations when using separate Label/Input components.
