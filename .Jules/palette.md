## 2026-03-19 - [Add role='alert' to error messages]
**Learning:** Form error messages should use `role='alert'` to ensure they are immediately announced by screen readers. This is a recurring pattern in the app's components (e.g. AddAppointmentDialog, AddPatientDialog, NoteEditor).
**Action:** Add `role='alert'` to all form error message containers.
